# FuelEU Compliance Platform

## Overview
A platform for FuelEU Maritime compliance, with React frontend and Node.js backend.

## Setup
1. Clone the repo.
2. Run `npm install` in both `/frontend` and `/backend`.
3. Start the backend: `npm run dev`.
4. Start the frontend: `npm run dev`.

## Tests
- Run `npm test` in both folders.
